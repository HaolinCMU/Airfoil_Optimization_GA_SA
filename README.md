# Airfoil_Optimization_GA_SA
This project is implementing genetic algorithm (GA) and simulated annealing (SA) to optimize airfoil's lift-to-drag ratio based on the pre-trained CNN (as the evaluation function)
